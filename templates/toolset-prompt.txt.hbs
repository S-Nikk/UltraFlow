====== ULTRAFLOW TOOLSET ACTIVATION PROMPT ======

You now have access to the Ultraflow integrated AI toolset.

AVAILABLE COMPONENTS:

{{#if (includes features 'Brain memory')}}
1. BRAIN MEMORY (MCP Server)
   Status: ACTIVATED
   Tools:
   - search_memories(query: string) -> Memory[]
     Search the brain vault for memories matching your query
   - get_memory(id: string) -> Memory
     Retrieve a specific memory by ID
   - list_memories() -> Memory[]
     List all available memories
   - refresh_index() -> void
     Update the memory index

   How to use:
   - Call search_memories() at the start of related work
   - Load relevant memories before making decisions
   - References: .claude/memory/, .claude/memory/vault.json
{{/if}}

{{#if (includes features 'Claude Flow orchestration')}}
2. CLAUDE FLOW ORCHESTRATION (CLI)
   Status: ACTIVATED
   Tools:
   - swarm-init(topology: string) -> Swarm
     Initialize a swarm with specified topology
   - swarm-add-agent(type: string) -> Agent
     Add specialized agents to active swarm
   - memory-store(key: string, value: any) -> void
     Store shared context in swarm memory

   How to use:
   - Use swarm-init() for complex multi-step tasks
   - Add specialized agents per problem domain
   - Share context via memory-store() for agent coordination
{{/if}}

{{#if (includes features 'OpenCode delegation')}}
3. OPENCODE DELEGATION
   Status: ACTIVATED
   Function: delegateToOpenCode(prompt: string, options?: object) -> Result

   Available options:
   - refactor: boolean - Task is code refactoring
   - test: boolean - Generate tests
   - optimize: boolean - Performance optimization
   - language: string - Target language (typescript, python, etc.)
   - timeout: number - Execution timeout in seconds

   How to use:
   - Use for large code generation tasks (100+ lines)
   - Offload test writing and refactoring
   - Include detailed requirements in prompt
{{/if}}

SESSION HOOKS (Auto-Running):

1. SessionStart Hook
   Automatically loads relevant memories at session start
   - Detects active project from git status
   - Searches for matching memories
   - Loads context proactively

2. UserPromptSubmit Hook
   Detects when you reference past work
   - Monitors for context indicators: "previous", "task", "continue", etc.
   - Suggests matching memories automatically
   - Triggers search_memories() if context detected

3. PostToolUse Hook
   Auto-saves session context to brain vault
   - Captures task progress
   - Updates memory index
   - Stores session summary

ACTIVATION CHECKLIST:

✓ Brain memory system: BUNDLED & READY
  Directory: .claude/memory/
  Vault file: .claude/memory/vault.json
  Auto-backup: enabled

✓ MCP servers registered: .claude/settings.json
  Brain: configured
{{#if (includes features 'Claude Flow orchestration')}}  Claude Flow: configured{{/if}}

✓ Hooks configured & active:
  SessionStart: ready
  UserPromptSubmit: monitoring
  PostToolUse: auto-saving

{{#if (includes features 'Claude Flow orchestration')}}
✓ Claude Flow: INSTALLED
  CLI version: 3.5.0+
  Location: npm global
{{/if}}

{{#if (includes features 'OpenCode delegation')}}
✓ OpenCode: AVAILABLE
  Binary detected in PATH
  Ready for delegation
{{/if}}

QUICK START:

1. Begin work:
   Start by searching brain: search_memories("your task keywords")

2. Load context:
   Use returned memories to understand prior work

3. Execute multi-agent tasks:
   Use Claude Flow for complex coordination: swarm-init("hierarchical")

4. Delegate code work:
   Use OpenCode for generation/refactoring: delegateToOpenCode("generate API client...")

5. Continue sessions:
   Brain automatically loads relevant context on session restart

MEMORY ORGANIZATION:

- Conscious Memory: .claude/memory/ (active memories)
- Vault: .claude/memory/vault.json (long-term storage)
- Session Context: .claude/memory/current-session.md (auto-updated)
- Index: .claude/memory/MEMORY.md (auto-maintained)

FEATURES:

{{#each features}}
- {{this}}
{{/each}}

SUPPORT:

Documentation: https://github.com/anthropics/ultraflow
Issues: Report via GitHub
Updates: npm update ultraflow

---
Generated by Ultraflow v1.0.0
Activate this toolset by pasting the above into your AI's system prompt or conversation context.
